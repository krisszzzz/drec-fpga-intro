diff --git a/hw/arm/virt.c b/hw/arm/virt.c
index 5fa045c..5de45a4 100644
--- a/hw/arm/virt.c
+++ b/hw/arm/virt.c
@@ -85,6 +85,7 @@
 #include "hw/virtio/virtio-iommu.h"
 #include "hw/char/pl011.h"
 #include "qemu/guest-random.h"
+#include "hw/misc/mul-dev.h"
 
 static GlobalProperty arm_virt_compat[] = {
     { TYPE_VIRTIO_IOMMU_PCI, "aw-bits", "48" },
@@ -171,6 +172,7 @@ static const MemMapEntry base_memmap[] = {
     /* This redistributor space allows up to 2*64kB*123 CPUs */
     [VIRT_GIC_REDIST] =         { 0x080A0000, 0x00F60000 },
     [VIRT_UART0] =              { 0x09000000, 0x00001000 },
+    [VIRT_MUL_DEV] =            { 0x09009000, 0x00001000 },
     [VIRT_RTC] =                { 0x09010000, 0x00001000 },
     [VIRT_FW_CFG] =             { 0x09020000, 0x00000018 },
     [VIRT_GPIO] =               { 0x09030000, 0x00001000 },
@@ -2093,6 +2095,12 @@ static void virt_cpu_post_init(VirtMachineState *vms, MemoryRegion *sysmem)
     }
 }
 
+static void create_mul_dev(VirtMachineState *vms)
+{
+    hwaddr base = vms->memmap[VIRT_MUL_DEV].base;
+    sysbus_create_simple("mul-dev", base, NULL);
+}
+
 static void machvirt_init(MachineState *machine)
 {
     VirtMachineState *vms = VIRT_MACHINE(machine);
@@ -2399,6 +2407,7 @@ static void machvirt_init(MachineState *machine)
 
     vms->highmem_ecam &= (!firmware_loaded || aarch64);
 
+    create_mul_dev(vms);
     create_rtc(vms);
 
     create_pcie(vms);
diff --git a/hw/misc/Kconfig b/hw/misc/Kconfig
index 1f1baa5..a4293b3 100644
--- a/hw/misc/Kconfig
+++ b/hw/misc/Kconfig
@@ -1,3 +1,7 @@
+config MUL_DEVICE
+    bool
+    default y
+
 config APPLESMC
     bool
     depends on ISA_BUS
diff --git a/hw/misc/meson.build b/hw/misc/meson.build
index d02d96e..807ff43 100644
--- a/hw/misc/meson.build
+++ b/hw/misc/meson.build
@@ -24,6 +24,7 @@ system_ss.add(when: 'CONFIG_IOSB', if_true: files('iosb.c'))
 
 # virt devices
 system_ss.add(when: 'CONFIG_VIRT_CTRL', if_true: files('virt_ctrl.c'))
+system_ss.add(when: 'CONFIG_MUL_DEVICE', if_true: files('mul-dev.c'))
 
 # RISC-V devices
 system_ss.add(when: 'CONFIG_MCHP_PFSOC_DMC', if_true: files('mchp_pfsoc_dmc.c'))
diff --git a/hw/misc/mul-dev.c b/hw/misc/mul-dev.c
new file mode 100644
index 0000000..a41f71a
--- /dev/null
+++ b/hw/misc/mul-dev.c
@@ -0,0 +1,65 @@
+#include "qemu/osdep.h"
+#include "qemu/log.h"
+
+#include "hw/misc/mul-dev.h"
+
+static uint64_t mul_dev_read(void *opaque, hwaddr addr, unsigned len)
+{
+    MulDev *dev = opaque;
+
+    if (addr != 0x0)
+        return 0;
+
+    return dev->lhs * dev->rhs;
+}
+
+static void mul_dev_write(void *opaque, hwaddr addr, uint64_t val,
+                          unsigned len)
+{
+    MulDev *dev = opaque;
+
+    if (addr == 0x8)
+        dev->lhs = val;
+    else if (addr == 0x10)
+        dev->rhs = val;
+}
+
+static const MemoryRegionOps mul_dev_ops = {
+    .read  = mul_dev_read,
+    .write = mul_dev_write,
+    .endianness = DEVICE_LITTLE_ENDIAN,
+};
+
+static void mul_dev_init(Object *obj)
+{
+    MulDev *s = MUL_DEV(obj);
+    SysBusDevice *sbd = SYS_BUS_DEVICE(obj);
+    qemu_log("%s: enter\n", __func__);
+    s->lhs = 0;
+    s->rhs = 0;
+    memory_region_init_io(&s->iomem, OBJECT(s), &mul_dev_ops, s,
+                          TYPE_MUL_DEV, MUL_DEV_SIZE);
+    sysbus_init_mmio(sbd, &s->iomem);
+}
+
+static void mul_dev_class_init(ObjectClass *klass, void *data)
+{
+    DeviceClass *dc = DEVICE_CLASS(klass);
+    dc->desc = TYPE_MUL_DEV;
+    set_bit(DEVICE_CATEGORY_MISC, dc->categories);
+}
+
+static const TypeInfo mul_dev_info = {
+    .name           = TYPE_MUL_DEV,
+    .parent         = TYPE_SYS_BUS_DEVICE,
+    .instance_size  = sizeof(MulDev),
+    .instance_init  = mul_dev_init,
+    .class_init     = mul_dev_class_init,
+};
+
+static void inc_dev_register_types(void)
+{
+    type_register_static(&mul_dev_info);
+}
+
+type_init(inc_dev_register_types)
diff --git a/include/hw/arm/virt.h b/include/hw/arm/virt.h
index aca4f80..51031bc 100644
--- a/include/hw/arm/virt.h
+++ b/include/hw/arm/virt.h
@@ -63,6 +63,7 @@ enum {
     VIRT_GIC_REDIST,
     VIRT_SMMU,
     VIRT_UART0,
+    VIRT_MUL_DEV,
     VIRT_MMIO,
     VIRT_RTC,
     VIRT_FW_CFG,
diff --git a/include/hw/misc/mul-dev.h b/include/hw/misc/mul-dev.h
new file mode 100644
index 0000000..957b6f6
--- /dev/null
+++ b/include/hw/misc/mul-dev.h
@@ -0,0 +1,15 @@
+#include "hw/sysbus.h"
+#include "exec/memory.h"
+#include "qom/object.h"
+
+#define MUL_DEV_SIZE 0x1000
+
+struct MulDev {
+    SysBusDevice parent_obj;
+    MemoryRegion iomem;
+    uint64_t lhs;
+    uint64_t rhs;
+};
+
+#define TYPE_MUL_DEV "mul-dev"
+OBJECT_DECLARE_SIMPLE_TYPE(MulDev, MUL_DEV)
